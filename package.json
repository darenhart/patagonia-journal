{
  "name": "patagonia-journal",
  "version": "1.0.0",
  "description": "Paragônia Bike Trip - Journal",
  "main": "README.md",
  "scripts": {
    "build": "npm run markdown-to-html && npm run add-markdown-css && npm run copy-images",
    "dev": "npm run build && google-chrome dist/index.html",
    "copy-images": "rm dist/img -r && cp img/ dist/ -r",
    "markdown-to-html": "cat template.html > dist/index.html && marked < README.md >> dist/index.html && cat footer.html >> dist/index.html",
    "add-markdown-css": "cp node_modules/github-markdown-css/github-markdown.css dist/",
    "deploy": "git add dist && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/darenhart/patagonia-journal.git"
  },
  "author": "Daniel Werle Arenhart",
  "license": "ISC",
  "dependencies": {
    "github-markdown-css": "^4.0.0",
    "marked": "^0.8.0"
  }
}
